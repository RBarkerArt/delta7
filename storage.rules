rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // All paths: Public read, Admin SDK only for writes
    match /{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
